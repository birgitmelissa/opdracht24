function add(nr1, nr2) {
    return nr1 + nr2;
  }
  
const result = add(4, 8);
console.log(result)

const optellen = function (number1, number2) {
    return number1 + number2;
  };
  
const uitkomst = optellen(4, 8);
console.log(uitkomst)

const agregar = (n1, n2) => {
    return n1 + n2;
  };

const salir = agregar(4, 8);
console.log(salir)